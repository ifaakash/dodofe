const BlobChart = ({ data }: { data: { reels: number, posts: number, stories: number } }) => {
    return (
        <div>
            <h1>BlobChart</h1>
        </div>
    )
}

export default BlobChart